import { Column, TextField } from "@/components";

export const Home = () => (
    <Column>

        <TextField
            label = "Serviços"
            inputProps={{placeholder:"Digite seu nome"}}
        />

    </Column>
)